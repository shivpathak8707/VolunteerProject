{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "java",
      "name": "Launch MainApp",
      "request": "launch",
      "mainClass": "com.volunteerapp.MainApp",
      "classPaths": [
        "${workspaceFolder}/out",
        "${workspaceFolder}/lib/*"
      ]
    }
  ]
}
